
version = rootProject.mod_version

base {
    archivesName = "thermoo-patches-overflowing-bars-patch"
}

dependencies {
    api project(path: ':thermoo-patches-base')

    compileOnly "maven.modrinth:overflowing-bars:${project.overflowingbars_version}"
}