
version = rootProject.mod_version

base {
    archivesName = "thermoo-patches-fabric-seasons-patch"
}

dependencies {
    modCompileOnly "maven.modrinth:fabric-seasons:${project.fabric_seasons_version}"
}