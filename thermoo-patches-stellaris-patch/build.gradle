version = rootProject.mod_version

base {
    archivesName = "thermoo-patches-stellaris-patch"
}

dependencies {
    api project(path: ':thermoo-patches-base', configuration: 'namedElements')

    modCompileOnly "maven.modrinth:stellaris:${project.stellaris_version}"
}