
version = rootProject.mod_version

base {
    archivesName = "thermoo-patches-colorfulhearts-patch"
}

dependencies {
    api project(path: ':thermoo-patches-base', configuration: 'namedElements')

    modCompileOnly "maven.modrinth:colorful-hearts:${project.colorfulhearts_version}"
}