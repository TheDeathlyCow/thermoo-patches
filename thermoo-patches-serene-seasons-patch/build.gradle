
version = rootProject.mod_version

base {
    archivesName = "thermoo-patches-serene-seasons-patch"
}

dependencies {
    api project(path: ':thermoo-patches-base')

    compileOnly "maven.modrinth:serene-seasons:${project.serene_seasons_version}"
}