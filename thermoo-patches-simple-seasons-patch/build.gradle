
version = rootProject.mod_version

base {
    archivesName = "thermoo-patches-simple-seasons-patch"
}

dependencies {
    api project(path: ':thermoo-patches-base', configuration: 'namedElements')

    modCompileOnly "maven.modrinth:simple-seasons:${project.simple_seasons_version}"
}